{
  "name": "ch02",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.12.2",
    "parcel-bundler": "^1.8.1"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run app\"",
    "app": "parcel index.html --open",
    "server": "json-server --watch db.json"
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  }
}
